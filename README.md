# Smart-Attendance-Management-System---Computer-Vision
Python &amp; OpenCV powered solution for CS402.3 (NSBM). Processes physical signing sheet images via image pipeline (grayscale, binarization, cell detection) to detect signatures, maps records via info.xml, stores data in SQLite, displays stats via infovis.py, &amp; verifies signatures via investigate.py.
